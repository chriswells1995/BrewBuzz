$("#headerName").on("click", function() {
    var query = $(brewery);
    console.log("BREWERYHERE", brewery)
    // return if they didn't put anything in the form-controlbox
    return query;
    });